<h1>List of tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <% if task.completed? %>
    <li> [x] <%= link_to task.title, task %>  </li>
    <% else %>
    <li> [ ] <%= link_to task.title, task %></li>
    <% end %>
 <% end %>
</ul>
